# EXP-0056 — M6-A12 Qwen3.8-27B attention projections

## Status

KEEP — composed projection slice complete; attention state and full layer
execution remain outside this milestone.

## Question

Can the validated layer-3 qwen35 prefix fan out through the real Q, K, and V
projections and K normalization using the GGUF contracts?

## Baseline

Baseline commit: `f2fdb75` (M6-A11).

Model:

* `/home/fedora-workstation/models/Qwen3.8-27B-Q4_K_M.gguf`
* SHA-256: `7e78da5d7e3ae28d178121f58646953305f3e5bd3cb46f4a75584e8b6c6fe169`
* layer: `3`, full attention

Reference fixture: `/tmp/m6a1-qwen38-reference`, generated by the pinned
llama.cpp reference used by M6-A1. The input is `0-l_out-2-30.f32`; expected
checkpoints are `0-Qcur_full-3-32.f32`, `0-Kcur_normed-3-39.f32`, and
`0-Vcur-3-35.f32`.

## Candidate

Compose:

```text
layer input → RMSNorm → one shared Q8_K input
  → Q4_K×Q8_K Q projection
  → Q4_K×Q8_K K projection → per-head K normalization
  → Q6_K×Q8_K V projection
```

The qwen35 diagnostic path uses the actual layer-3 V type, Q6_K. The old
qwen3 production executor and kernel selection are unchanged.

## Result

```text
model=Qwen3.8-27B
architecture=qwen35
device=AMD Instinct MI60 / MI50 (gfx906:sramecc+:xnack-)
layer=3 full-attention projections
operations=RMSNorm->Q8_K->{Q,K,V}->KNorm
v_type=Q6_K
q_max_abs_error=2.86102e-06
k_norm_max_abs_error=9.53674e-07
v_max_abs_error=1.90735e-06
M6-A12 qwen35 attention projections PASS
```

All compared values were finite and within the declared diagnostic
tolerances.

## Checks

* real MI50 composed projection tool: PASS
* host CTest: 8/8
* MI50 CTest: 19/19
* `git diff --check`: PASS

## Decision

KEEP. The real qwen35 full-attention Q/K/V projection contracts now execute in
one diagnostic path, including K normalization. This is not full attention or
end-to-end GPU generation support.

## Follow-up

M6-A13 should extend the layer-3 path through Q/K normalization, sectioned
RoPE, and KV-cache append before implementing the attention reduction.
