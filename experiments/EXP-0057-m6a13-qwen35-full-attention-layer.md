# EXP-0057 — M6-A13 Qwen3.8-27B full-attention GPU layer

## Status

KEEP — complete layer-3 full-attention vertical slice; diagnostic GPU path
only. No qwen35 production executor is selected.

## Question

Can a real layer-3 input advance through the complete Qwen3.8-27B
full-attention layer on gfx906 and match the pinned external reference at the
layer boundary?

## Baseline

Baseline commit: `704fbae` (M6-A12).

Model:

* `/home/fedora-workstation/models/Qwen3.8-27B-Q4_K_M.gguf`
* SHA-256: `7e78da5d7e3ae28d178121f58646953305f3e5bd3cb46f4a75584e8b6c6fe169`
* architecture: `qwen35`
* layer: `3` (full attention)

Reference fixture: `/tmp/m6a1-qwen38-reference`, generated by the pinned
llama.cpp reference used by M6-A1. Inputs and checkpoints cover positions
`0..8`.

## Candidate

The diagnostic path allocates persistent device buffers once, uploads
immutable weights once, and executes:

```text
layer input
  → attention RMSNorm → Q8_K
  → Q/K/V projections
  → Q/K head normalization
  → sectioned RoPE
  → persistent KV-cache append
  → cached attention
  → sigmoid attention gate
  → output projection
  → attention residual
  → FFN RMSNorm → Q8_K
  → Gate/Up projections
  → SiLU/multiply → Q8_K
  → Down projection
  → layer residual output
```

The real layer-3 contracts are used: Q/K and FFN projections are Q4_K,
V is Q6_K, and the output projection is Q4_K. Existing qwen3 production
behavior is unchanged.

## Results

```text
positions checked: 0..8
device: AMD Instinct MI60 / MI50 (gfx906:sramecc+:xnack-)
max_attention_error: 0.00292516
max_ffn_error:       0.00455475
max_layer_error:     0.00516891
```

All checked values were finite and within the diagnostic tolerance. The full
layer output matched the external checkpoint at every tested position.

## Correctness

* Q/K/V, K normalization, RoPE, attention, gated attention, output projection,
  post-attention norm, FFN, and final layer output checked on real fixtures.
* Stateful KV-cache mutation exercised across positions `0..8`.
* Persistent device buffers used for the complete diagnostic loop.
* No production token trajectory or throughput claim is made by this tool.

## Checks

* real MI50 full-layer tool: PASS
* host CTest: 8/8
* MI50 CTest: 19/19
* `git diff --check`: PASS

## Decision

KEEP. Layer 3 is now a trustworthy externally checked full-attention vertical
slice through its output boundary. This closes M6-A13 and supplies the
attention-layer component needed for the layers-0–3 hybrid-block audit.

## Follow-up

M6-A14 should add state fingerprints and poisoned-state reset/replay checks.
After that, compose layers 0–3 as the first stateful qwen35 hybrid block,
checking recurrent state, layer-3 KV state, and block output at positions
`1, 2, 4, 8, 16`.
