# EXP-0055 — M6-A11 Qwen3.8-27B composed attention prefix

## Status

KEEP — composed diagnostic slice complete; full qwen35 layer execution remains
outside this milestone.

## Question

Can the validated qwen35 RMSNorm and Q4_K×Q8_K projection primitives compose
into a real layer-3 GPU prefix and match external checkpoints?

## Baseline

Baseline commit: `e8c934c` (M6-A10).

Model:

* `/home/fedora-workstation/models/Qwen3.8-27B-Q4_K_M.gguf`
* SHA-256: `7e78da5d7e3ae28d178121f58646953305f3e5bd3cb46f4a75584e8b6c6fe169`
* layer: `3`, full attention

Reference fixture: `/tmp/m6a1-qwen38-reference`, generated by the pinned
llama.cpp reference used by M6-A1. The input is
`tensors/0-l_out-2-30.f32`; expected checkpoints are
`tensors/0-attn_norm-3-31.f32` and `tensors/0-Qcur_full-3-32.f32`.

## Candidate

Add a standalone diagnostic path:

```text
layer input → RMSNorm → Q8_K quantization → Q4_K×Q8_K attn_q
```

The existing qwen3 production executor and kernel selection are unchanged.

## Result

```text
model=Qwen3.8-27B
architecture=qwen35
device=AMD Instinct MI60 / MI50 (gfx906:sramecc+:xnack-)
layer=3 full-attention prefix
operations=RMSNorm->Q8_K->Q4_KxQ8_K attn_q
norm_max_abs_error=0
q_max_abs_error=2.86102e-06
M6-A11 qwen35 attention prefix PASS
```

All compared values were finite and within the declared diagnostic
tolerances.

## Checks

* real MI50 composed-prefix tool: PASS
* `git diff --check`: PASS

## Decision

KEEP. Two validated qwen35 GPU primitives now compose across a real layer
boundary. This is not full qwen35 GPU-layer or end-to-end generation support.

## Follow-up

M6-A12 should extend this layer-3 prefix through the remaining full-attention
projection and state operations, one externally checked slice at a time.
